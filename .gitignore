# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Windows NTFS Alternate Data Stream markers (created when files come from another origin)
*Zone.Identifier*

# Claude Code local settings (per-machine)
.claude/settings.local.json

# Raw data spreadsheets (already imported to Firestore)
ARROLAMENTO_PLANILHA_*.xlsx

# Firebase service account keys (must never be committed)
service-account.json
*serviceAccount*.json
